A simple Spring MVC application that takes user input and checks the input by using standard validation annotations. Displays an error message so that the user can re-enter input to make it be valid.
